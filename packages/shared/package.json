{
  "name": "@grotec/shared",
  "version": "0.1.0",
  "private": true,
  "description": "GROTEC FarmerOS CRM — domain constants, enums, permission codes shared by API and Web",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "~5.6.3",
    "vitest": "^2.1.8"
  }
}
